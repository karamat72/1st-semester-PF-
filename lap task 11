Task 1:
 Write a program that overloads a function to calculate the volume of different
3D shapes:
 Volume of a cube (side length as input).
 Volume of a cylinder (radius and height as input).

#include <iostream>
using namespace std;

// Function to calculate volume of a cube
double volume(double side)
{
    return side * side * side;
}

// Function to calculate volume of a cylinder
double volume(double radius, double height)
{
    return 3.14159 * radius * radius * height;
}

int main()
{
    double side, radius, height;

    // Cube
    cout << "Enter side length of cube: ";
    cin >> side;
    cout << "Volume of Cube = " << volume(side) << endl;

    // Cylinder
    cout << "\nEnter radius of cylinder: ";
    cin >> radius;
    cout << "Enter height of cylinder: ";
    cin >> height;
    cout << "Volume of Cylinder = " << volume(radius, height) << endl;

    return 0;
}




 Create a function that accepts a string as input and returns the number of
vowels in the string. Overload the function to also return the number of consonants.




#include <iostream>
#include <string>
using namespace std;

// Function to count vowels
int countLetters(string str)
{
    int vowels = 0;

    for (int i = 0; i < str.length(); i++)
    {
        char ch = tolower(str[i]);

        if (ch == 'a' || ch == 'e' || ch == 'i' || 
            ch == 'o' || ch == 'u')
        {
            vowels++;
        }
    }
    return vowels;
}

// Overloaded function to count consonants
int countLetters(string str, bool countConsonants)
{
    int consonants = 0;

    for (int i = 0; i < str.length(); i++)
    {
        char ch = tolower(str[i]);

        if (ch >= 'a' && ch <= 'z')
        {
            if (!(ch == 'a' || ch == 'e' || ch == 'i' ||
                  ch == 'o' || ch == 'u'))
            {
                consonants++;
            }
        }
    }
    return consonants;
}

int main()
{
    string text;

    cout << "Enter a string: ";
    getline(cin, text);

    cout << "Number of Vowels = " << countLetters(text) << endl;
    cout << "Number of Consonants = " << countLetters(text, true) << endl;

    return 0;
}

Task 3
Write a program that calculates the average of an array of numbers. Overload
the function to handle both integer arrays and floating-point arrays. Submit the code
and screenshots of the output.
#include <iostream>
using namespace std;

// Function to calculate average of integer array
double average(int arr[], int size)
{
    int sum = 0;
    for (int i = 0; i < size; i++)
    {
        sum += arr[i];
    }
    return (double)sum / size;
}

// Overloaded function to calculate average of float array
double average(float arr[], int size)
{
    float sum = 0;
    for (int i = 0; i < size; i++)
    {
        sum += arr[i];
    }
    return sum / size;
}

int main()
{
    // Integer array
    int intArr[5] = {10, 20, 30, 40, 50};

    // Floating-point array
    float floatArr[5] = {2.5, 3.5, 4.5, 5.5, 6.5};

    cout << "Average of Integer Array = "
         << average(intArr, 5) << endl;

    cout << "Average of Float Array = "
         << average(floatArr, 5) << endl;

    return 0;
}


